route:
  receiver: local-webhook
  group_by:
    - alertname
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://webhook:8080/
        send_resolved: true

        